apiVersion: v1
kind: Service
metadata:
  name: cast-db
  namespace: staging
  labels:
    app: cast-db
    environment: staging
    component: database
spec:
  type: ClusterIP
  ports:
  - name: postgres
    port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: cast-db
    environment: staging