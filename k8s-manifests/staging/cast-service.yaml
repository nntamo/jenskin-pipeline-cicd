apiVersion: v1
kind: Service
metadata:
  name: cast-service
  namespace: staging
  labels:
    app: cast-service
    environment: staging
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: cast-service
    environment: staging
