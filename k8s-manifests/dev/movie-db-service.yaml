apiVersion: v1
kind: Service
metadata:
  name: movie-db
  namespace: dev
  labels:
    app: movie-db
    environment: dev
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: movie-db
    environment: dev
