apiVersion: v1
kind: Service
metadata:
  name: cast-service
  namespace: dev
  labels:
    app: cast-service
    environment: dev
    component: api
spec:
  type: ClusterIP
  ports:
  - name: http
    port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: cast-service
    environment: dev