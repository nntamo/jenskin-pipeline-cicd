apiVersion: v1
kind: Service
metadata:
  name: movie-service
  namespace: prod
  labels:
    app: movie-service
    environment: prod
spec:
  type: ClusterIP
  ports:
  - port: 8000
    targetPort: 8000
    protocol: TCP
  selector:
    app: movie-service
    environment: prod
