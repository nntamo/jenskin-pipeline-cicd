apiVersion: v1
kind: Service
metadata:
  name: cast-db
  namespace: prod
  labels:
    app: cast-db
    environment: prod
spec:
  type: ClusterIP
  ports:
  - port: 5432
    targetPort: 5432
    protocol: TCP
  selector:
    app: cast-db
    environment: prod
